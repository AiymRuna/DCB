@model IEnumerable<WebApplication1.Models.TerminalLocation>

@{
    ViewBag.Title = "Локации";
}

<h2>Локации</h2>

<p>
    @Html.ActionLink("Добавить", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TerminalInf.NomerTerminal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Longth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Width)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TerminalInf.NomerTerminal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Longth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Width)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Просмотреть", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
