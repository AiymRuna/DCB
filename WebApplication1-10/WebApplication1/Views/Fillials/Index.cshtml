@model IEnumerable<WebApplication1.Models.Fillial>

@{
    ViewBag.Title = "Филиалы";
}

<h2>Филиалы</h2>

<p>
    @Html.ActionLink("Добавить", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NmFilial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oblast.NmOblast)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NmFilial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oblast.NmOblast)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Просмотреть", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
